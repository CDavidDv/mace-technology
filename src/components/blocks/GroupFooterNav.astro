---
export interface Props {
    className?: string;
}

const  { className } = Astro.props ;
---

<div class=`col-span-1 h-auto flex flex-col grid grid-cols-2 gap-8 h-max ${className}`>
    <slot/>
</div>